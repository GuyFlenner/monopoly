# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/
htmlcov/
.coverage
.coverage.*

# Node / web
node_modules/
packages/web/dist/
packages/web/.vite/
packages/web/*.tsbuildinfo
packages/web/coverage/
packages/web/test-results/
packages/web/playwright-report/
openapi.json   # regenerated on demand; packages/web/src/api/generated.ts IS committed
               # so CI can diff it against a fresh generation and catch contract drift

# Env / secrets
.env
.env.*
!.env.example

# Saved games produced by local play sessions
*.savegame.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# VSCode Local History extension artifacts
.history/
.claude/worktrees/

# The Pyodide wheels the local build installs (MON-805). Build artifacts, not sources: the deploy
# workflow builds them straight into the site, and a developer checking the local build does the
# same by hand — see docs/DEPLOYMENT.md.
packages/web/public/wheels/
