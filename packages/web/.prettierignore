dist
tsconfig.tsbuildinfo

# MON-302: generated by `openapi-typescript` from the server's OpenAPI document, and the CI
# `contract` job diffs the committed file against a fresh generation byte for byte. Running
# prettier over it would make that diff fail forever — the two gates would be enforcing
# different files. Nobody edits this by hand, so nobody reads its formatting.
src/api/generated.ts

# Playwright run output, not authored files.
test-results
playwright-report

# MON-731: `vitest --coverage`'s v8/istanbul HTML report — regenerated by the command CI itself
# runs, and already in the root .gitignore, but prettier does not read that file, only this one.
coverage

# MON-805: the Pyodide wheels and their manifest are build artifacts. The deploy workflow writes
# them straight into the site and `.gitignore` keeps them out of the repository, but they sit under
# `public/` while a local build is being checked — where prettier would otherwise report the
# generated manifest as unformatted and fail a gate over a file nobody wrote.
public/wheels
