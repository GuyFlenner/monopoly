---
name: "_checkpoint_20260804_120935"
description: "Session checkpoint — post-sign-off improvement run complete, development signed off — 2026-08-04"
metadata:
  type: project
---

## Session Checkpoint — 2026-08-04T12:09:35+03:00

**Task**: After the 2026-08-03 sign-off, explore the published product for remaining defects, spec them,
fix them, and close every open gap. Development is now **signed off** from the implementer's side.
**Branch**: `main` (clean; every branch merged)
**PR**: none open — #39, #40, #42, #43, #44, #45, #46 all merged today
**Phase completed**: All of it. Every backlog item is done except the four MON-90x rows, which are
explicitly out of v1 scope.
**Next action**: Nothing is required. Two loops are outstanding and neither blocks: read the re-measured
mutation kill-rate from the nightly dispatched against `fix/mon-722-tests-where-mutants-survived`
(GitHub Actions → Nightly), and decide whether to take MON-722's remaining legality-predicate cluster.
The most productive next move is not in the backlog: **play the published game and report what
irritates** — that route produced every item below.

### Open HITL blockers

none. **D1 is closed** — the one blocker this project carried (save-then-load refused in one sitting)
was decided by the owner and shipped as MON-714/ADR-011: a load whose game is still live now asks
whether to replace it or seat it separately.

### Open advisories

- 🟡 **Branch protection excludes administrators** — still true; a docs commit once reached `main`
  bypassing the required check. Owner-side setting, cannot be done from a session.
- 🟡 **`.claude/worktrees/` holds ~590 MB** of gitignored junk; `rm -rf` is hook-blocked, so the owner
  has to clear it.
- 🟡 **MON-722's legality-predicate cluster** (`_build_house` 13 surviving mutants, `_trade_side` 11,
  `_sell_house` 7, `_unmortgage` 6) is filed and deliberately not run to zero. It wants a deliberate
  session at the Fable tier with the printed rules open, not a sweep.
- 🟡 **ADR-011's WebSocket limitation** is *accepted*, not deferred: a watcher of a replaced game goes
  quiet, and a close code alone makes it worse (the watcher reconnects at its own cursor, the replaced
  log restarts at `seq 1`, so it receives silence). Revisit trigger: **MON-901**.

### Key decisions made this session

- **ADR-011 — a save is an envelope, and a load may take over an id.** `{state, events}`; a bare
  `GameState` still loads. `if_exists=refuse|replace|copy`, defaulting to `refuse` so the unchanged
  request still answers the 409 it always did, and the UI asks only *after* the refusal.
- **The currency is per locale, and the format spec is named per string** — `$50` / `50 ₪`. Per string
  because `{{minimum}}` is money in a bid error and a player count in a seating error; only the
  sentence knows.
- **`requiresConfirmation` takes the ruleset.** Declining a purchase confirms only when auctions are on;
  `ACTION_THEME` still classes it `terminal`, because that is its cost when there is an auction to lose
  the tile to.
- **The Israeli board's default lives in the engine** (`board/loader.py::PREFERRED_BOARDS`), not as a
  hardcoded id in the UI — the setup screen already took "the first the server offered".
- **A Hebrew setup screen defaults to the masculine**, which amends owner decision 5 / GAP G-42 for the
  *default only*; the neutral fallback and the English default are unchanged and asserted.
- **The mutation gate is trusted because it was measured, not because it is green.** 3270/3471 = 94.2%
  in 87 minutes proves the mutants are imported and killed; `mutate_only_covered_lines` because a
  mutant on an untested line measures coverage, not strength; and MON-722's tests were each verified by
  hand-mutating the implementation and watching them go red.
